<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5" />
    <title>استوديوهات كادر - CADER STUDIOS</title>
    
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
      /* --- تعديلات الموبايل فقط (لن تؤثر على الحاسوب) --- */
      @media (max-width: 768px) {
        /* منع التمرير الأفقي والمشاكل الجانبية */
        html, body, #root { overflow-x: hidden !important; max-width: 100vw !important; width: 100% !important; margin: 0; padding: 0; }
        
        /* تقليل الهوامش والمسافات الكبيرة للأقسام */
        section, div[id="tour"], div[id="contact"] { padding: 3rem 1.5rem !important; }
        
        /* تصغير العناوين لتناسب الشاشة */
        h1 { font-size: 1.8rem !important; line-height: 1.3 !important; }
        h1 span { font-size: 1.5rem !important; }
        h2 { font-size: 1.6rem !important; }
        h3 { font-size: 1.3rem !important; }
        p { font-size: 0.95rem !important; line-height: 1.6 !important; }
        
        /* تحجيم الصور والفيديو */
        img, video, iframe { max-width: 100% !important; height: auto !important; border-radius: 12px !important; }
        header img, nav img, footer img { height: 50px !important; width: auto !important; }
        
        /* تحويل الشبكات (العناصر بجانب بعضها) إلى عمود واحد */
        div[style*="grid-template-columns"] { grid-template-columns: 1fr !important; gap: 1.5rem !important; }
        
        /* تحسين صفحة التواصل (الفورم) */
        form { display: flex !important; flex-direction: column !important; gap: 1rem !important; }
        input, select, textarea, button { width: 100% !important; box-sizing: border-box !important; font-size: 16px !important; /* لمنع الزووم في الآيفون */ }
        
        /* تحسينات لصناديق اللوكيشنات والمميزات */
        .about-box, .meeting-room-box, .contact-box, .form-box { padding: 1.5rem !important; margin-top: 1.5rem !important; }
        
        /* شريط التواصل السفلي */
        .contact-banner { font-size: 1.1rem !important; padding: 1rem !important; flex-direction: column; text-align: center; }
      }
    </style>

    <script type="module" crossorigin src="/assets/index-b1szlymd.js"></script>
    <link rel="stylesheet" crossorigin href="/assets/index-d971oz7u.css">
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
